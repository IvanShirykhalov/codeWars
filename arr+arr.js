function arrayPlusArray(arr1, arr2) {
  let Arr1 = arr1.map((i) => (x += i), (x = 0)).reverse()[0];
  let Arr2 = arr2.map((i) => (x += i), (x = 0)).reverse()[0];
  return Arr1 + Arr2; //something went wrong
}
